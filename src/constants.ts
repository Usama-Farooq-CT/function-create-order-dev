let GCP_FUNCTION = "GCF"
let AWS_LAMBDA = "lambda"
let AZURE_FUNCTION = "azure"
let WORKER_FUNCTION = "worker"
let DEV_ENV = "dev"

export {
  GCP_FUNCTION,
  AWS_LAMBDA,
  AZURE_FUNCTION,
  WORKER_FUNCTION,
  DEV_ENV
}
